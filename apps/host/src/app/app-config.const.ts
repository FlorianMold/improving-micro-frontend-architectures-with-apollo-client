/** The name of the app. */
export const UI_HOST_APP_NAME = 'host';

/** The remote applications that the host can load. */
export const enum UI_HOST_REMOTES {
  CONTACT = 'contact',
  SALES = 'sales',
  DASHBOARD = 'dashboard',
  USER = 'user',
}
