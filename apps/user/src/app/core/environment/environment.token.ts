import { InjectionToken } from '@angular/core';
import { UiUserEnvironment } from './environment.type';

export const UI_USER_ENVIRONMENT = new InjectionToken<UiUserEnvironment>('ui-user-environment');
