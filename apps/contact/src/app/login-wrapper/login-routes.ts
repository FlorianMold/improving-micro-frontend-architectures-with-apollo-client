import { Routes } from '@angular/router';
import { UiContactLoginWrapperComponent } from './login-wrapper.component';

export const LOGIN_ROUTES: Routes = [
  {
    path: '',
    component: UiContactLoginWrapperComponent,
  },
];
