import { InjectionToken } from '@angular/core';
import { UiContactEnvironment } from './environment.type';

export const UI_CONTACT_ENVIRONMENT = new InjectionToken<UiContactEnvironment>('ui-contact-environment');
