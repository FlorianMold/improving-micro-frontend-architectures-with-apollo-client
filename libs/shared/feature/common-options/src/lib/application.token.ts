import { InjectionToken } from '@angular/core';

/** Injection token that can be used for providing the name of the application. */
export const UI_APPLICATION_NAME = new InjectionToken<string>('ui-application-name');
